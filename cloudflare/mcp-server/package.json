{
  "name": "numap-mcp-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "NU Map MCP server on Cloudflare Workers — Durable Object per session, same port adapters as the Node dev server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
