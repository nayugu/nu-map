[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  # Top-level only; recursive was tripping a "No url found" git error.
  GIT_SUBMODULE_STRATEGY = "normal"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
