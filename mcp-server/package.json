{
  "name": "nu-map-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for NU Map — gives Claude read/write access to the course catalog, requirements, and live plan state",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "zod": "^3.24.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
