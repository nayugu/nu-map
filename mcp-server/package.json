{
  "name": "nu-map-mcp",
  "version": "2.0.0",
  "type": "module",
  "description": "MCP server for NU Map — gives Claude deep read access to the catalog, requirements, offerings, and live plan state; changes are propose-for-review by default",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/unit.test.mjs",
    "test:e2e": "node test/e2e.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "ws": "^8.18.0",
    "zod": "^3.24.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
